// import { useEffect } from 'react';
// import { toast } from 'react-toastify';

// function ToastHandler() {
//   useEffect(() => {
//     const message = window.toastMessage;
//     const type = (window.toastType || 'info').toLowerCase();

//     if (message) {
//       if (type === 'success') toast.success(message, { position: "top-right", autoClose: 3000 });
//       else if (type === 'error') toast.error(message, { position: "top-right", autoClose: 3000 });
//       else if (type === 'warn') toast.warn(message, { position: "top-right", autoClose: 3000 });
//       else toast.info(message, { position: "top-right", autoClose: 3000 });
//     }
//   }, []);

//   return null;
// }

// export default ToastHandler;
